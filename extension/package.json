{
  "name": "jat12-extension",
  "version": "12.0.0",
  "private": true,
  "type": "module",
  "description": "JAT 12 thin MV3 extension — stateless hands & eyes (sensor snapshots up, actuator commands down). Zero runtime deps; @jat12/shared is bundled in by esbuild. Load-unpacked target: extension/dist.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.330"
  }
}
