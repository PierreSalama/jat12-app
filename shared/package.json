{
  "name": "@jat12/shared",
  "version": "12.0.0",
  "private": true,
  "type": "module",
  "description": "Contract package imported by app AND extension AND tests: wire protocol, adapter schema, status vocabulary, constants, normalizers. The single source of truth that makes contract-drift a compile error.",
  "main": "src/index.ts",
  "comment:exports": "Points at .ts source (internal-package pattern): app/extension/tests resolve types with no build step. ./protocol + ./adapter-schema are added in M1 when those modules land.",
  "exports": {
    ".": "./src/index.ts",
    "./constants": "./src/constants.ts",
    "./norm": "./src/norm.ts",
    "./contracts/status.json": "./src/contracts/status.json"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
